[package]
name = "pixels-authorizer"
version = "0.1.0"
edition = "2021"

[dependencies]
ed25519-compact = { version = "2", default-features = false }

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
